/** Calendar view - displays schedule blocks. Placeholder for now. */

import "./CalendarView.css";


export default function CalendarView() {
  return (
    <div className="calendar-view">
      <h2>Schedule</h2>
      <div className="calendar-placeholder">
        <p>Calendar view will display generated schedule blocks.</p>
      </div>
    </div>
  );
}
